if defined?(Schemagic)
  Schemagic.configure do |config|
    config.env = Rails.env
    config.data_folder = Rails.root.join("app", "schema")
    config.schema_url = "https://raw.githubusercontent.com/[USERNAME]/[REPO]/master/app/schema"
  end
end
